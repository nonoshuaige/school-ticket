# 校园活动票务系统 — 开发说明书

> 版本：v3.0（笔记 + 关注 + 点赞 + UI 改版）
> 最后更新：2026-06-21
> 开发模式：全栈自研（Vue 3 + Spring Boot + MySQL）
>
> GitHub 项目首页：[README.md](./README.md)

---

## 一、项目概述

### 1.1 项目背景

面向毕业晚会、歌手大赛等校内热门活动的票务开售场景，构建一套从前端售票、订单创建、支付关单、取消回补到退款补偿的订单履约闭环系统。v3.0 新增笔记社区（发笔记、点赞、关注）丰富用户互动。

### 1.2 核心功能

| 模块 | 功能 |
|------|------|
| 用户 | 注册、登录（手机号+密码，Cookie+JWT）、退出、关注/取关、关注统计 |
| 活动 | 首页双列网格、热卖中/预热中分组（按时间窗口）、真实最低票价、分页 |
| 购票 | 选择活动 → 选择票类 → 确认下单（名额扣减）→ 支付（模拟） |
| 订单 | 创建→支付→取消→退款→核销，五态流转，后端分页+状态筛选 |
| 我的 | 个人信息、关注数/粉丝数、订单入口 |
| 笔记 | 双列小红书风格卡片、封面占位图、点赞/取消、关注/取关、分页 |
| 导航 | 底部三 Tab：购票、笔记、我的 |

---

## 二、技术栈与环境

### 2.1 后端

| 组件 | 实装版本 | 说明 |
|------|---------|------|
| JDK | 21.0.2 LTS (OpenJDK) | Java 21 |
| Spring Boot | 3.2.5 | 内置 Tomcat |
| Spring Security | 6.x | Cookie + JWT 无状态认证 |
| MyBatis-Plus | 3.5.7 | ORM，含分页插件 |
| MySQL | 8.0.28（阿里云镜像） | 生产库 |
| Maven | 3.9.6 | 项目构建 |
| Lombok | 1.18.x | 减少样板代码 |
| Hutool | 5.8.29 | 工具库（Snowflake 订单号） |
| JJWT | 0.12.5 | JWT 生成与验证 |

### 2.2 前端

| 组件 | 版本 | 说明 |
|------|------|------|
| Node.js | 20.14.0 LTS | 构建运行环境 |
| Vue | 3.4+ (Composition API) | 前端框架 |
| Vite | 5.x | 构建工具，开发期代理 |
| Vue Router | 4.x | 前端路由（Hash 模式） |
| Pinia | 2.x | 状态管理 |
| Axios | 1.x | HTTP 请求（withCredentials） |
| Vant | 4.x | 移动端 UI 组件库 |

### 2.3 开发环境安装目录

所有工具统一安装在 `D:\JavaDevelop\` 下：

```
D:\JavaDevelop\
├── jdk-21\                     # JDK 21
├── maven-3.9.6\                # Maven 3.9.6
├── mysql-8.0.28-winx64\        # MySQL 8.0.28
├── node-20.14.0\               # Node.js 20 LTS
└── repository\                 # Maven 本地仓库
```

环境变量（用户级别）：

| 变量 | 值 |
|------|-----|
| JAVA_HOME | D:\JavaDevelop\jdk-21 |
| MAVEN_HOME | D:\JavaDevelop\maven-3.9.6 |
| Path 追加 | %JAVA_HOME%\bin; %MAVEN_HOME%\bin; D:\JavaDevelop\node-20.14.0; D:\JavaDevelop\mysql-8.0.28-winx64\bin |

---

## 三、系统架构

### 3.1 认证流程

1. 登录成功 → 后端将 JWT 写入 httpOnly Cookie（name=token）
2. 前端 Axios 自动附带 Cookie（withCredentials: true）
3. JWT Cookie 过滤器拦截请求 → 解析 Cookie 中的 token → 提取 userId → 写入 CurrentUserHolder
4. 业务层通过 CurrentUserHolder.getUserId() 获取当前用户
5. 请求结束 → CurrentUserHolder.remove() 清除 ThreadLocal

### 3.2 CurrentUserHolder

```java
public class CurrentUserHolder {
    private static final ThreadLocal<Long> USER_ID_HOLDER = new ThreadLocal<>();
    public static void setUserId(Long userId) { USER_ID_HOLDER.set(userId); }
    public static Long getUserId() { return USER_ID_HOLDER.get(); }
    public static void remove() { USER_ID_HOLDER.remove(); }
}
```

> 约定：禁止在 Controller 参数中传递 userId。统一从 CurrentUserHolder 获取。

### 3.3 笔记模块认证

笔记列表接口为公开访问（`permitAll()`），但点赞和关注操作需要登录。Controller 层使用 `tryGetUserId()` 和 `requireUserId()` 两个辅助方法区分：
- `tryGetUserId()`：静默获取，未登录返回 null，列表仍正常返回但不带当前用户状态
- `requireUserId()`：强制获取，未登录抛出 `BusinessException(401, "请先登录")`

---

## 四、数据库设计

### 4.1 表结构（7 张表）

**user 表** — 100 条测试数据
```sql
CREATE TABLE user (
  user_id     BIGINT       AUTO_INCREMENT PRIMARY KEY,
  phone       VARCHAR(20)  NOT NULL UNIQUE,
  password    VARCHAR(255) NOT NULL,
  nickname    VARCHAR(50)  DEFAULT '',
  create_time DATETIME     DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE
);
```

**event 表** — 15 场活动
```sql
CREATE TABLE event (
  event_id         BIGINT       AUTO_INCREMENT PRIMARY KEY,
  title            VARCHAR(100) NOT NULL,
  description      TEXT,
  organizer        VARCHAR(100) NOT NULL,
  venue            VARCHAR(100) NOT NULL,
  poster_url       VARCHAR(255),
  sale_start_time  DATETIME     NOT NULL,
  sale_end_time    DATETIME     NOT NULL,
  event_start_time DATETIME     NOT NULL,
  event_end_time   DATETIME     NOT NULL,
  status           TINYINT      DEFAULT 0,
  create_time      DATETIME     DEFAULT CURRENT_TIMESTAMP,
  update_time      DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE
);
-- status: 0=预热中, 1=热卖中, 2=已结束
-- Event.java 含 @TableField(exist=false) minPrice，由 Service 层填充
```

**ticket_category 表** — 每活动 2-3 档，共 44 条
```sql
CREATE TABLE ticket_category (
  ticket_id          BIGINT        AUTO_INCREMENT PRIMARY KEY,
  event_id           BIGINT        NOT NULL,
  name               VARCHAR(50)   NOT NULL,
  price              DECIMAL(10,2) NOT NULL,
  total_quantity     INT           NOT NULL,
  remaining_quantity INT           NOT NULL,
  description        VARCHAR(255),
  create_time        DATETIME      DEFAULT CURRENT_TIMESTAMP,
  update_time        DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE,
  FOREIGN KEY (event_id) REFERENCES event(event_id)
);
```

**order 表**
```sql
CREATE TABLE `order` (
  id            BIGINT        AUTO_INCREMENT PRIMARY KEY,
  order_no      VARCHAR(64)   NOT NULL UNIQUE,
  user_id       BIGINT        NOT NULL,
  ticket_id     BIGINT        NOT NULL,
  quantity      INT           DEFAULT 1,
  total_price   DECIMAL(10,2) NOT NULL,
  status        TINYINT       DEFAULT 0,
  expire_time   DATETIME      NOT NULL,
  create_time   DATETIME      DEFAULT CURRENT_TIMESTAMP,
  paid_time     DATETIME,
  cancel_time   DATETIME,
  refund_time   DATETIME,
  use_time      DATETIME,
  update_time   DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE,
  INDEX idx_user_status (user_id, status),
  FOREIGN KEY (user_id) REFERENCES user(user_id),
  FOREIGN KEY (ticket_id) REFERENCES ticket_category(ticket_id)
);
```

**user_follow 表** — 关注关系，295 条测试数据
```sql
CREATE TABLE user_follow (
  id           BIGINT   AUTO_INCREMENT PRIMARY KEY,
  follower_id  BIGINT   NOT NULL,
  followee_id  BIGINT   NOT NULL,
  create_time  DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_follow (follower_id, followee_id),
  INDEX idx_follower (follower_id),
  INDEX idx_followee (followee_id),
  FOREIGN KEY (follower_id) REFERENCES user(user_id),
  FOREIGN KEY (followee_id) REFERENCES user(user_id)
);
```

**user_note 表** — 笔记，200 条测试数据
```sql
CREATE TABLE user_note (
  note_id     BIGINT       AUTO_INCREMENT PRIMARY KEY,
  user_id     BIGINT       NOT NULL,
  content     VARCHAR(500) NOT NULL,
  create_time DATETIME     DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user_id (user_id),
  INDEX idx_create_time (create_time),
  FOREIGN KEY (user_id) REFERENCES user(user_id)
);
```

**note_like 表** — 点赞，496 条测试数据
```sql
CREATE TABLE note_like (
  id          BIGINT   AUTO_INCREMENT PRIMARY KEY,
  note_id     BIGINT   NOT NULL,
  user_id     BIGINT   NOT NULL,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_note_user (note_id, user_id),
  INDEX idx_note_id (note_id),
  FOREIGN KEY (note_id) REFERENCES user_note(note_id),
  FOREIGN KEY (user_id) REFERENCES user(user_id)
);
```

### 4.2 建表/删表顺序

外键约束决定了 DROP 顺序（子表先删，父表后删）：

```
DROP: note_like → user_follow → `order` → ticket_category → user_note → event → user
CREATE: user → event → ticket_category → `order` → user_follow → user_note → note_like
```

### 4.3 测试数据

| 表 | 数量 | 生成方式 |
|-----|------|---------|
| user | 100 | gen_data.js 随机中文名 |
| event | 15 | 手动定义 #1-#15 |
| ticket_category | 44 | gen_data.js 随机价格+库存 |
| user_note | 200 | gen_data.js 随机分配 |
| user_follow | 295 | gen_data.js 随机去重 |
| note_like | 496 | gen_data.js 随机去重 |

> 数据生成脚本：`backend/gen_data.js`，运行 `node gen_data.js` 输出 `gen_data.sql`，手动拼入 `init.sql`。

### 4.4 测试账号

手机号：`13800138000` 密码：`123456` 昵称：测试用户（user_id=1）

---

## 五、订单状态机与履约流程

### 5.1 状态流转

```
[*] → 待支付(0) → 已支付(1) → 已核销(4)
待支付(0) → 已取消(2)  [主动取消/超时关单]
已支付(1) → 已退款(3)  [申请退款]
```

### 5.2 关键规则

**下单（名额扣减）**
- 事务内：SELECT ... FOR UPDATE 锁定票档行 → 校验余量 → UPDATE remaining_quantity -= quantity → 插入订单（status=0, expire_time=NOW()+15min）

**支付**（模拟）
- 验证订单存在且 status=0 且未过期 → 置 status=1, paid_time=NOW()

**取消/超时关单（回补）**
- 主动取消：POST /api/v1/order/cancel
- 超时关单：@Scheduled(fixedRate=30s) 扫描 expire_time < NOW() AND status=0
- 回补：UPDATE remaining_quantity += quantity, status=2

**退款**
- POST /api/v1/order/refund → 回补名额 + status=3

---

## 六、后端 API 接口

基础路径：`/api/v1`  认证：Cookie 中携带 `token`

### 6.1 用户

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /auth/register | 注册 |
| POST | /auth/login | 登录（JWT 写入 Cookie） |
| POST | /auth/logout | 退出（Cookie 过期） |
| GET | /user/info | 当前用户信息 |

### 6.2 活动

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /event/list?page=&pageSize=&status= | 活动列表（分页，含 minPrice） |
| GET | /event/{eventId} | 活动详情+票档 |
| GET | /event/{eventId}/tickets | 票档列表 |

> `getEventList()` 返回 `IPage<Event>`，每条 Event 含 `minPrice`（批量查询 ticket_category 取最低价）。默认每页 8 条，按 event_start_time 升序。

### 6.3 订单

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /order/create | 创建订单 |
| POST | /order/pay | 模拟支付 |
| POST | /order/cancel | 取消订单 |
| POST | /order/refund | 申请退款 |
| POST | /order/use | 核销 |
| GET | /order/list?page=&pageSize=&status= | 订单列表（后端分页+状态筛选） |
| GET | /order/{orderNo} | 订单详情 |

### 6.4 关注

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /user/{followeeId}/follow | 关注用户（需登录） |
| DELETE | /user/{followeeId}/follow | 取消关注（需登录） |
| GET | /user/follow/stats | 当前用户关注数/粉丝数 |
| GET | /user/follow/check/{followeeId} | 检查当前用户是否已关注 |

### 6.5 笔记

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /note/list?page=&pageSize= | 笔记列表（公开，可选登录态） |
| POST | /note/{noteId}/like | 点赞（需登录） |
| DELETE | /note/{noteId}/like | 取消点赞（需登录） |

> 笔记列表返回字段：noteId, content, createTime, likeCount, isLiked, userId, nickname, phone。已登录时 isLiked 反映真实状态，未登录时为 false。默认每页 10 条。

### 6.6 响应格式

```json
{ "code": 200, "msg": "success", "data": {...} }
{ "code": 4001, "msg": "余票不足", "data": null }
```

### 6.7 分页响应格式

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "records": [...],
    "total": 15,
    "size": 8,
    "current": 1,
    "pages": 2
  }
}
```

---

## 七、后端目录结构

```
backend/
├── pom.xml
├── init.sql                       # 完整建表+测试数据（7表，~1300行）
├── gen_data.js                    # Node.js 测试数据生成脚本
└── src/main/java/com/schoolticket/
    ├── SchoolTicketApplication.java
    ├── config/
    │   ├── SecurityConfig.java    # 放行 /note/** 公开访问
    │   ├── WebMvcConfig.java
    │   └── MyBatisPlusConfig.java # PaginationInnerInterceptor(DbType.MYSQL)
    ├── common/
    │   ├── Result.java
    │   ├── BusinessException.java # 含 ALREADY_FOLLOWING(4006)、CANNOT_FOLLOW_SELF(4007)
    │   ├── GlobalExceptionHandler.java
    │   └── CurrentUserHolder.java
    ├── auth/
    │   ├── JwtUtil.java
    │   ├── AuthController.java
    │   └── AuthService.java
    ├── user/
    │   ├── entity/User.java
    │   ├── entity/UserFollow.java
    │   ├── mapper/UserMapper.java
    │   ├── mapper/UserFollowMapper.java
    │   ├── service/UserService.java
    │   ├── service/UserFollowService.java
    │   └── controller/UserController.java
    │   └── controller/UserFollowController.java
    ├── event/
    │   ├── entity/Event.java      # 含 @TableField(exist=false) minPrice
    │   ├── entity/TicketCategory.java
    │   ├── mapper/EventMapper.java
    │   ├── mapper/TicketCategoryMapper.java
    │   ├── service/EventService.java   # getEventList 返回 IPage + 填充 minPrice
    │   └── controller/EventController.java
    ├── order/
    │   ├── entity/Order.java
    │   ├── mapper/OrderMapper.java
    │   ├── service/OrderService.java
    │   ├── controller/OrderController.java
    │   └── task/OrderTimeoutTask.java
    ├── note/
    │   ├── entity/Note.java
    │   ├── entity/NoteLike.java
    │   ├── mapper/NoteMapper.java
    │   ├── mapper/NoteLikeMapper.java
    │   ├── service/NoteService.java
    │   └── controller/NoteController.java
    └── dto/
        ├── LoginReq.java
        ├── RegisterReq.java
        ├── OrderCreateReq.java
        └── OrderVO.java
```

---

## 八、前端页面与路由

| 路径 | 页面 | 底部导航 | 需登录 |
|------|------|---------|--------|
| / | 活动列表（双列网格） | 购票 | ❌ |
| /login | 登录 | — | ❌ |
| /register | 注册 | — | ❌ |
| /event/:id | 活动详情+票档 | — | ❌ |
| /order/confirm | 确认订单 | — | ✅ |
| /order/pay | 支付+倒计时 | — | ✅ |
| /order/result | 支付结果 | — | ❌ |
| /notes | 笔记社区（双列小红书风格） | 笔记 | ❌ |
| /mine | 我的（含关注统计） | 我的 | ❌ |
| /mine/orders | 我的订单（Tab+分页） | — | ✅ |
| /mine/detail/:orderNo | 票证详情 | — | ✅ |

### 8.1 页面功能要点

**EventList（活动列表）**
- 按时间窗口分组：🔥 热卖中（now 在 saleStart~saleEnd 之间）、⏳ 预热中（now < saleStart）
- 已结束活动自动隐藏
- 双列 CSS Grid 矩形卡片，显示 emoji、标题、日期、场馆、最低票价"¥XX起"
- 后端分页，每页 8 条，van-pagination 翻页

**Notes（笔记社区）**
- 双列 Grid 瀑布流布局
- 每张卡片：渐变色封面 + emoji 占位（8 种渐变循环）、内容两行截断、昵称+头像
- 底部显示发布时间 + 关注按钮，作者右侧显示点赞数
- 点赞/取消：红心 vs 空心，数字实时更新
- 关注/取关：按钮文字切换，toast 提示
- 未登录点操作按钮弹出"请先登录"提示
- 分页：每页 10 条

**Mine（我的）**
- 显示昵称、手机号
- 关注 X | 粉丝 X（调用 /user/follow/stats）

### 8.2 前端目录

```
frontend/
├── package.json / vite.config.js / index.html
└── src/
    ├── main.js / App.vue
    ├── api/
    │   ├── auth.js
    │   ├── event.js       # getEventList(status, page, pageSize)
    │   ├── order.js       # getOrderList({page, pageSize, status})
    │   ├── user.js        # followUser, unfollowUser, getFollowStats, checkFollowing
    │   └── note.js        # getNoteList, likeNote, unlikeNote
    ├── router/index.js    # 含 /notes 路由
    ├── stores/ (user.js / order.js)
    ├── utils/  (request.js / format.js)
    ├── components/
    │   ├── BottomNav.vue  # 三 Tab：购票(/), 笔记(/notes), 我的(/mine)
    │   └── EventCard.vue  # 双列矩形卡片，真实 minPrice
    └── views/
        ├── Login.vue / Register.vue
        ├── EventList.vue      # 双列网格 + 分组标题 + 分页
        ├── EventDetail.vue
        ├── OrderConfirm.vue / OrderPay.vue / OrderResult.vue
        ├── Notes.vue          # 双列小红书风格 + 点赞关注 + 时间显示
        ├── Mine.vue           # 个人信息 + 关注统计
        ├── MyOrders.vue       # Tab 筛选 + 后端分页
        └── TicketDetail.vue
```

---

## 九、运行指南

### 9.1 启动 MySQL

MySQL 安装路径：`D:\JavaDevelop\mysql-8.0.28-winx64\`，data 目录已初始化。

**启动方式**（以管理员身份运行 PowerShell）：
```powershell
Start-Process -FilePath "D:\JavaDevelop\mysql-8.0.28-winx64\bin\mysqld.exe" `
  -ArgumentList "--defaults-file=`"D:\JavaDevelop\mysql-8.0.28-winx64\my.ini`"" `
  -Verb RunAs -WindowStyle Hidden
```

> InnoDB 数据文件 `ibdata1` 需要写权限，直接双击 mysqld.exe 可能报 `The innodb_system data file 'ibdata1' must be writable`。已有 mysqld 进程残留时先 `taskkill /f /im mysqld.exe` 再启动。

**验证**：
```powershell
Get-Process -Name "mysqld" -ErrorAction SilentlyContinue
& "D:\JavaDevelop\mysql-8.0.28-winx64\bin\mysql.exe" -u root -p123456 -e "SELECT 1"
```

### 9.2 初始化数据库

```powershell
cmd /c "D:\JavaDevelop\mysql-8.0.28-winx64\bin\mysql.exe -u root -p123456 --default-character-set=utf8mb4 -D school_ticket < D:\MyProjrct\backend\init.sql"
```

> PowerShell 中 `<` 是保留运算符，需用 `cmd /c` 包装。init.sql 中 CREATE DATABASE / USE 被注释，必须加 `-D school_ticket`。

**验证数据**：
```powershell
& "D:\JavaDevelop\mysql-8.0.28-winx64\bin\mysql.exe" -u root -p123456 -D school_ticket -e "SELECT COUNT(*) FROM user; SELECT COUNT(*) FROM event; SELECT COUNT(*) FROM ticket_category; SELECT COUNT(*) FROM user_note; SELECT COUNT(*) FROM user_follow; SELECT COUNT(*) FROM note_like;"
```

期望输出：100 用户、15 活动、44 票档、200 笔记、295 关注、496 点赞。

### 9.3 启动后端（Spring Boot）

```powershell
cd D:\MyProjrct\backend
D:\JavaDevelop\maven-3.9.6\bin\mvn.cmd spring-boot:run
```

或打包后运行：
```powershell
mvn package -DskipTests
java -jar target/school-ticket-1.0.0.jar --spring.profiles.active=dev
```

启动成功标志：
```
Tomcat started on port 8080 (http)
Started SchoolTicketApplication in X.XXX seconds
```

### 9.4 启动前端（Vite + Vue 3）

```powershell
cd D:\MyProjrct\frontend
D:\JavaDevelop\node-20.14.0\npm.cmd install    # 仅首次 / 依赖变更时
D:\JavaDevelop\node-20.14.0\npm.cmd run dev
```

启动成功标志：
```
VITE v5.4.21  ready in XXXX ms
➜  Local:   http://localhost:5173/
```

### 9.5 服务总览

| 服务 | 地址 | 端口 |
|------|------|------|
| MySQL | localhost | 3306 |
| 后端 API | http://localhost:8080 | 8080 |
| 前端页面 | http://localhost:5173 | 5173 |

### 9.6 测试账号

`13800138000` / `123456`（昵称：测试用户）

---

## 附 A：开发问题记录

| 问题 | 解决 |
|------|------|
| 下单 403 | JWT Cookie Filter 补上 SecurityContextHolder.setAuthentication() |
| MySQL 8.0.36 CDN 不可达 | 改用阿里云镜像 8.0.28 |
| JDBC charset=utf8mb4 报错 | 改为 characterEncoding=UTF-8 |
| 前端无限跳回首页 | Pinia storeToRefs() 解构获取响应式 ref |
| 订单列表未区分状态 | 加 Tab 筛选 + 后端分页+状态筛选 |
| 未开售可购买 | isOnSale 计算属性控制票档禁用 |
| mysqld 启动报 ibdata1 must be writable | 管理员权限运行 PowerShell，`Start-Process -Verb RunAs` |
| PowerShell `mysql < init.sql` 报 RedirectionNotSupported | `<` 是 PS 保留运算符，改用 `cmd /c` 包装 |
| init.sql 执行报 No database selected | CREATE DATABASE / USE 被注释，加 `-D school_ticket` |
| pom.xml 自引用依赖导致 Maven 构建失败 | 删除 `<dependency>com.schoolticket:school-ticket</dependency>` |
| 活动卡片价格硬编码 | Event entity 加 @TableField(exist=false) minPrice，Service 批量查 ticket_category 取 MIN(price) |
| 笔记 N+1 查询用户信息 | 使用 userMapper.selectBatchIds() 批量加载 |
| 笔记点赞 N+1 查询 | LambdaQueryWrapper.in(noteIds) 批量查 + 按 noteId 分组 |
| gen_data.js 覆盖测试账号 | user_id=1 显式插入 '13800138000' / '测试用户' |
| 关注列表/笔记/点赞未落库 | 建表 + entity + mapper + service + controller 全套后端 |

## 附 B：Git 仓库与推送

### B.1 远程仓库

| 项目 | 值 |
|------|-----|
| 仓库地址 | https://github.com/nonoshuaige/school-ticket |
| 默认分支 | main |
| Git 路径 | D:\JavaDevelop\PortableGit\bin\git.exe |

### B.2 Git 环境

```
git config --global user.name "nonoshuaige"
git config --global user.email "3215357634@qq.com"
git config --global credential.helper manager
```

### B.3 .gitignore

```gitignore
# Maven
backend/target/
backend/.idea/
*.jar
*.class

# Node
frontend/node_modules/
frontend/dist/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.iml
.vscode/

# Env
.env
.env.local
```

### B.4 日常推送

```powershell
cd D:\MyProjrct
D:\JavaDevelop\PortableGit\bin\git.exe status
D:\JavaDevelop\PortableGit\bin\git.exe add .
D:\JavaDevelop\PortableGit\bin\git.exe commit -m "描述你的改动"
D:\JavaDevelop\PortableGit\bin\git.exe push
```

---

*本说明书随项目迭代更新。*
