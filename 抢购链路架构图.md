# 星席校内活动票务履约系统

```mermaid
graph TB
    %% ═══════════════════════════════════════
    %% 样式定义
    %% ═══════════════════════════════════════
    classDef gateway fill:#1a1a2e,stroke:#e94560,color:#fff
    classDef cache fill:#16213e,stroke:#f5a623,color:#fff
    classDef lua fill:#0f3460,stroke:#00d2ff,color:#fff
    classDef mq fill:#1a1a2e,stroke:#7b2ff7,color:#fff
    classDef db fill:#16213e,stroke:#4caf50,color:#fff
    classDef status fill:#0d1117,stroke:#8b949e,color:#fff

    %% ═══════════════════════════════════════
    %% 一、多级流量过滤
    %% ═══════════════════════════════════════
    subgraph L1["🔰 多级流量过滤 — 入口拦截无效流量，保障零超卖"]
        direction TB

        A[🐴 抢购请求<br/>POST /order/create]:::gateway

        B[① 用户级限流<br/>RateLimiter<br/>ConcurrentHashMap<br/>每人1s/次]:::gateway

        C[② Caffeine 售罄短路<br/>Cache Long→Boolean<br/>5s TTL 仅存已售罄<br/>未命中=未售罄]:::cache

        D[③ 单飞锁防击穿<br/>tryLock per-ticketId<br/>抢到→查Redis→写Caffeine<br/>没抢到→快速失败<br/>200并发仅1个查Redis]:::cache

        A --> B
        B -->|通过| C
        C -->|未售罄| D
        C -->|本地命中售罄| H0[❌ 本地短路<br/>不进入 Redis/Lua]:::status
        B -->|限流拦截| H0
        D -->|锁竞争失败| H0
        D -->|放行| I
    end

    %% ═══════════════════════════════════════
    %% 二、多维限购原子校验
    %% ═══════════════════════════════════════
    subgraph L2["📐 多维限购原子校验 — Lua内完成校验+扣减+留痕+写单，无锁化"]
        direction TB

        I["❄️ Snowflake 全局自增ID<br/>1bit符号 + 41bit毫秒时间戳 + 5bit数据中心<br/>+ 5bit机器ID + 12bit序列号<br/>预生成订单号，不依赖DB自增"]:::lua

        J["⚡ purchase.lua 原子执行 (≈12µs)<br/>Redis 单线程不可打断<br/>────<br/>① GET soldout → 二次安全校验<br/>② GET stock → 库存确认<br/>③ HGET event:purchase:{eid} {uid}<br/>   → 活动级限购校验 ≤5张<br/>④ DECRBY stock 扣库存<br/>⑤ HINCRBY event:purchase:{eid} {uid} qty<br/>   活动级购买记录累加<br/>⑥ SET order:{no} = JSON(status=-1)<br/>   写入订单存根，前端免库渲染<br/>⑦ XADD stream:orders 留痕"]:::lua

        K["📱 返回订单存根<br/>status=-1 排队中<br/>前端蓝色'排队中'提示<br/>全程零DB穿透"]:::cache

        I --> J
        J --> K

        J -.->|返回-1 soldout| H1[❌ Redis售罄]:::status
        J -.->|返回-2 stock不足| H2[❌ Redis库存不足]:::status
        J -.->|返回-3 limit超限| H3[❌ 该活动限购5张]:::status
    end

    %% ═══════════════════════════════════════
    %% 三、异步投递桥接
    %% ═══════════════════════════════════════
    subgraph L3["📨 异步投递桥接 — Lua留痕 + 阻塞队列 + RabbitMQ可靠落库"]
        direction TB

        STREAM[("stream:orders<br/>Redis Stream<br/>MAXLEN ~10000")]:::mq

        BRIDGE["🔄 StreamToRabbitMQBridge<br/>────<br/>Reader XREAD BLOCK<br/>  → LinkedBlockingQueue(1000)<br/>  → Worker×4 take()<br/>  → 同时投递双队列：<br/>    ① order.create.queue（落库）<br/>    ② order.delay.queue（延时取消）<br/>  两者发布成功 → XACK<br/><br/>兜底: XPENDING 每5s扫描<br/>  卡住>5s消息<br/>  → XRANGE重新投递"]:::mq

        RMQ_CREATE[("🐰 RabbitMQ<br/>order.create.queue<br/>持久化队列+DXL死信")]:::mq

        RMQ_DELAY[("⏳ RabbitMQ<br/>order.delay.queue<br/>延时队列 TTL=15min<br/>超时未支付→自动取消")]:::mq

        CONSUMER["📥 OrderCreateConsumer<br/>@RabbitListener<br/>INSERT MySQL 唯一落库路径<br/>DuplicateKeyException 幂等跳过<br/>异常→重试→DXL"]:::mq

        REFRESH["♻️ 落库后刷新Redis订单缓存<br/>status: -1(排队中) → 0(待支付)<br/>填充真实createTime<br/>用户即可查询到可见订单"]:::cache

        STREAM --> BRIDGE
        BRIDGE -->|同时投递| RMQ_CREATE
        BRIDGE -->|同时投递| RMQ_DELAY
        BRIDGE -->|PEL兜底| STREAM
        RMQ_CREATE --> CONSUMER
        CONSUMER --> REFRESH
    end

    %% ═══════════════════════════════════════
    %% 四、并发状态冲突治理
    %% ═══════════════════════════════════════
    subgraph L4["🔒 并发状态冲突治理 — 状态机+乐观锁+兜底对齐"]
        direction LR

        SM["📋 订单状态机<br/>────<br/>-1 排队中 → 0 待支付<br/>0 待支付 → 1 已支付<br/>0 待支付 → 2 已取消<br/>1 已支付 → 3 已退款<br/>1 已支付 → 4 已核销<br/><br/>状态校验: validateStatus()<br/>仅允许单一前置状态流转"]:::status

        OPT["🔐 支付回调与关单碰撞<br/>────<br/>网关超时 → 关单方主动查单<br/>getOrder() 查DB真实状态<br/>若已支付→不再取消<br/>若已取消→逆向退款兜底"]:::status

        EVENTLOG["📝 本地消息表 order_event_log<br/>────<br/>取消/退款/超时<br/>  → MySQL事务内<br/>  UPDATE order + INSERT event_log<br/>  → OrderEventLogTask 每10s<br/>  rollback.lua 回滚Redis库存<br/>  成功ack 失败重试≤5次"]:::status

        SM --> OPT
        OPT --> EVENTLOG
    end

    %% ═══════════════════════════════════════
    %% 五、退款单表幂等
    %% ═══════════════════════════════════════
    subgraph L5["💳 退款单表实现幂等 — 唯一索引 + 异步兜底 + 指数退避 + 查单对齐"]
        direction TB

        REFUND_API["📤 退款申请 POST /order/refund<br/>INSERT refund(refund_id=order_no)<br/>唯一索引拦截重复点击<br/>→ 重复请求降级提示'已申请'"]:::db

        REFUND_TASK["⏱️ RefundTask 每10s消费<br/>扫描 refund WHERE status=0<br/>────<br/>执行实际退款:<br/>  DB回补库存+UPDATE status=3<br/>  INSERT order_event_log<br/>    → 驱动Redis回滚"]:::db

        RETRY["🔁 异步退款兜底策略<br/>────<br/>网络异常: retry_count逐次递增<br/>  指数退避 1s→2s→4s→8s<br/>  满5次→阻断告警 status=2<br/>业务失败: 阻断并告警<br/>  钉钉/企微webhook通知<br/>未知状态: 主动调用查单对齐<br/>  GET /order/{no} 确认真实状态<br/>  以DB为准覆盖异常结果"]:::db

        REFUND_API --> REFUND_TASK
        REFUND_TASK --> RETRY
    end

    %% ═══════════════════════════════════════
    %% 层级连接
    %% ═══════════════════════════════════════
    K -->|Lua XADD| STREAM
    REFRESH -->|"用户查询<br/>GET /order/{no}<br/>GET /order/list"| QUERY[📖 Redis 优先<br/>全程不穿透DB]:::cache

    %% ═══════════════════════════════════════
    %% Redis 核心 Key 一览
    %% ═══════════════════════════════════════
    subgraph KEYS["🗝️ 核心 Redis 数据结构"]
        direction LR
        K1["ticket:stock:{tid}<br/>String 库存数<br/>TTL=saleEndTime"]:::cache
        K2["ticket:soldout:{tid}<br/>String 售罄标记<br/>TTL=5min"]:::cache
        K3["event:purchase:{eid}<br/>Hash {uid:qty}<br/>持久 活动购买记录"]:::cache
        K4["order:{orderNo}<br/>String JSON<br/>TTL=1800s"]:::cache
        K5["stream:orders<br/>Stream<br/>MAXLEN~10000"]:::mq
    end
```

---

## 架构分层总结

| 层级 | 职责 | 关键技术 | 失败处理 |
|------|------|---------|---------|
| **多级流量过滤** | 入口拦截无效流量 | RateLimiter + Caffeine售罄 + 单飞锁 | 售罄即短路，锁竞争失败快速返回 |
| **多维限购原子校验** | 无锁化校验+扣减+写单 | Lua原子脚本(7操作/12µs) | 售罄-1/库存-2/限购-3 分层返回 |
| **异步投递桥接** | 可靠落库，双队列投递 | Stream + 阻塞队列 + RabbitMQ双队列 + PEL | 延时队列兜底取消 + 订单号幂等 |
| **并发状态冲突治理** | 支付回调vs关单碰撞 | 状态机+乐观锁+查单对齐 | 逆向退款兜底 |
| **退款单表幂等** | 防重复退款+异步兜底 | 唯一索引+指数退避+告警 | 重试5次→阻断告警 |
